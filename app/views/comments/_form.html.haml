= form_for [@user, @post, @comment || @post.comments.build] do |f|
  = render 'shared/error_messages', object: f.object
  .field
    = f.text_area :body, placeholder: "Compose new comment..."
  = f.submit "comment", class: "btn btn-large btn-primary"